# Node environment
NODE_ENV=development

# Server port (if you add an HTTP server later)
PORT=3000

# Database connection (optional — only if you add Postgres)
# DATABASE_URL=postgresql://user:password@localhost:5432/dbname
