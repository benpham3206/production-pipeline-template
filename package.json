{
  "name": "production-pipeline-template",
  "version": "1.0.0",
  "description": "A stack-agnostic, production-ready project scaffold for human + AI agent collaboration.",
  "scripts": {
    "test": "jest",
    "test:coverage": "jest --coverage",
    "lint": "eslint src && tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "init": "./scripts/init-project.sh",
    "health-check": "./scripts/health-check.sh",
    "pre-ship-audit": "./scripts/pre-ship-audit.sh",
    "checkpoint": "./scripts/checkpoint.sh",
    "recover": "./scripts/recovery.sh"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "@types/jest": "^29.5.12",
    "@types/node": "^20.11.0",
    "eslint": "^9.0.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "typescript": "^5.3.3",
    "typescript-eslint": "^8.0.0"
  }
}
